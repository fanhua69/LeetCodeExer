
#include "headers.h"

class CMaxProduct
{
    public:
        vector<int> maxp;
        vector<int> maxn;

        void maxProduct(vector<int> &nums, int i);
        int maxProduct(vector<int> &nums);
};